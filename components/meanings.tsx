import { FunctionalComponent } from "preact/src/index.d.ts";
import { Meanings } from "../utils/utils.ts";

type Props = {
    Meanings: Meanings
}

const Significados:FunctionalComponent<Props> = (props) => {
    const meanings = props.Meanings

    return(
        //hacer
        <div>
        
        </div>
    )
}

export default Significados;